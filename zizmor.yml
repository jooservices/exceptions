# zizmor policy for jooservices/exceptions workflows.
# Version tags (@v7) are acceptable; SHA pinning is enforced selectively.
rules:
  unpinned-uses:
    config:
      policies:
        "*": ref-pin
